# ~/exness-deploy/.env.server
# Copy to EC2: mkdir -p ~/exness-deploy && nano ~/exness-deploy/.env.server

# Redis (required – same as container name on exness-network)
REDIS_URL=redis://exness-redis:6379

# Optional: API port (default 4000)
# PORT=4000
