# Optional: override for local dev (Docker uses docker-compose env)
REDIS_URL=redis://localhost:6379
PORT=4000
WS_PORT=8080

# Web app (only needed when building/running web with custom API/WS URLs)
# NEXT_PUBLIC_BACKEND_URL=http://localhost:4000/api/v1
# NEXT_PUBLIC_WS_URL=ws://localhost:8080
